# @sk-task helm-chart#T5.1: Gateway Ingress (AC-007)
{{- if and .Values.gateway.ingress.enabled .Values.gateway.enabled (not .Values.all.enabled) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "maskchain.fullname" . }}-gateway
  labels:
    {{- include "maskchain.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.gateway.ingress.annotations | nindent 4 }}
spec:
  {{- with .Values.gateway.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  rules:
    - host: {{ .Values.gateway.ingress.host }}
      http:
        paths:
          - path: {{ .Values.gateway.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "maskchain.fullname" . }}-gateway
                port:
                  number: {{ .Values.configBase.server.port }}
  {{- with .Values.gateway.ingress.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
