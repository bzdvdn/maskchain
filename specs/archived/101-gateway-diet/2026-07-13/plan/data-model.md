# Data Model: 101-gateway-diet

## Status

- **no-change**

## Причина

Фича меняет только API-слой и сборку (build tags, Makefile). Domain-модели, репозитории, миграции, event-контракты и API DTO не затрагиваются.
